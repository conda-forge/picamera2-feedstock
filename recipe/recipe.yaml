context:
  version: "0.3.36"

package:
  name: picamera2
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/p/picamera2/picamera2-${{ version }}.tar.gz
    sha256: 3add10c8e5613234f39f271c90b886306e6fa4a64c99196a2451c308bd278d70

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - libcamera
    - pidng
    - av
    - jsonschema
    - pykms
    - python-libarchive-c
    - numpy
    - piexif
    - pillow
    - python-prctl
    - simplejpeg
    - tqdm
    - py-videodev2
    - openexr-python
    - __linux

tests:
  - python:
      imports:
        - picamera2
      pip_check: false
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script: run_test.py
    requirements:
      run:
        - python ${{ python_min }}.*

about:
  homepage: https://github.com/RaspberryPi/picamera2
  summary: The libcamera-based Python interface to Raspberry Pi cameras, based on the original Picamera library
  license: BSD-2-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - carlodri
